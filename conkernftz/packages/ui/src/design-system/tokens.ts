export const tokens = {
  colors: {
    background: 'var(--bg)',
    text: 'var(--text)',
    accent: 'var(--accent)',
  },
  shadow: {
    subtle: 'var(--shadow-subtle)',
  },
};

export type Tokens = typeof tokens;
