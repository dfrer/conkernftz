# Node
node_modules/

# Builds
dist/
dist-cjs/
build/
.turbo/
coverage/
*.tsbuildinfo

# OS / Editor
.DS_Store
Thumbs.db
*.log
.DS_Store
Thumbs.db

# Prevent stray compiled JS from ending up in src for storage
packages/storage/src/**/*.js
packages/storage/src/**/*.d.ts
packages/storage/src/**/*.map
.idea/
.vscode/

# Local keys and secrets
keys/
.env
.env.*


# UI screenshot harness output (dev artifact)
packages/ui/screenshots/

# Stale nested duplicate working copy (a leftover second checkout at <root>/conkernftz/,
# ~623 MB incl. its own node_modules — never tracked). Ignoring it keeps it out of git and
# stops Glob/Grep tooling from descending into the duplicate. Safe to delete on disk:
#   rm -rf conkernftz
/conkernftz/
